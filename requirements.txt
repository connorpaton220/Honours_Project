# Curation-only dependencies. Training stack is separate.
# Pinned to recent-stable versions known to work with Gemma 3 tokenizer.
transformers>=4.50.0
datasets>=3.0.0
huggingface_hub>=0.25.0
sentencepiece>=0.2.0
protobuf>=4.25.0
tqdm>=4.66.0
